using UnityEngine;

public class GoogleLogInScript : MonoBehaviour {
    /*
    static string access_token = "380685054111-8ib2c4fs9nls6njlp953q4dn98cskh25.apps.googleusercontent.com";
	Firebase.Auth.FirebaseAuth auth = Firebase.Auth.FirebaseAuth.DefaultInstance;
	Firebase.Auth.Credential credential;

	private void Start() {
		credential = Firebase.Auth.GoogleAuthProvider.GetCredential(access_token, null);
		
		auth.SignInWithCredentialAsync(credential).ContinueWith(task => {
			if(task.IsCompleted && !task.IsCanceled && !task.IsFaulted) {
				// User is now signed in.
				Firebase.Auth.FirebaseUser newUser = result.Result();
			}
			// Use this for initialization
		});
	}
    */
}